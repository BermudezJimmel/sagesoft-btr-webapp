version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install --production --silent
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
