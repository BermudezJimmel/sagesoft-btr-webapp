version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install --force
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
